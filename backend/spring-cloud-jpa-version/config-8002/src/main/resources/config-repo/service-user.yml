server:
  port: 7001
  tomcat:
    uri-encoding: UTF-8
    max-swallow-size: -1

spring:
    application:
      name: service-user
#    profiles:
#      active: dev
#    config:
#      import:  "optional:configserver:localhost:8002"